name: smart_islamic_opinions
description: الجامع الذكي لآراء المذاهب الإسلامية - منصة عرض واستعراض
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  url_launcher: ^6.2.0   # ✅ إضافة حزمة فتح البريد الإلكتروني

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
